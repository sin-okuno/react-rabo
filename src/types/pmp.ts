
export type KnowledgeAreaType = {
  id: string;
  knowledgeArea: string;
};

export type ProcessType = {
  id: string;
  processName: string;
  processGroup: string;
};

export type IttoType = {
  id: string;
  input: string;
  tool: string;
  output: string;
};
